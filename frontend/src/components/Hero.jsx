import "../styles/hero.css"

export default function Hero() {
  return (
    <section className="hero">
      <div className="hero-overlay">
        <h1>Find your next stay</h1>
        <p>Discover unique places around the world</p>
      </div>
    </section>
  )
}
